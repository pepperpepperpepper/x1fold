# Disable the internal IPU6 camera stack by default (power + hibernate stability).
#
# Enable on-demand with: sudo x1fold-webcam-on
# Disable again with:    sudo x1fold-webcam-off
blacklist intel_ipu6
blacklist intel_ipu6_isys
blacklist ipu_bridge
blacklist ov5675

